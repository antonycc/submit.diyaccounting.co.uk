// And the bundles records will have a grace period where the APIs permit traffic but the activity buttons show disabled.
// There is a salted hash of the user sub (email?) salt stored as an environment secret
